/**
 * Dashboard entry for vendor telemetry UI.
 * Implementation lives in VendorDashboard; this alias matches the product surface name.
 */
export { default } from '@/components/VendorDashboard';
